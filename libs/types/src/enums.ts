/**
 * This enum represents the available themes for the app.
 */
export enum ThemeID {
  LIGHT = 'light',
  DARK = 'dark',
}

/**
 * This enum represents the available locales for the app.
 */
export enum Locale {
  EN_US = 'en-US',
  FA_IR = 'fa-IR',
}

/**
 * This enum represents the available text directions for the app.
 */
export enum Direction {
  LTR = 'ltr',
  RTL = 'rtl',
}
