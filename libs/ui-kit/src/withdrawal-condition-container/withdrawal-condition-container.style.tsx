import styled from 'styled-components';

export const WithdrawalConditionContainer = styled.div`
  .radio-group {
    width: 100%;
  }

  .title {
    color: ${(p) => p.theme.textPrimary};
    font-weight: 700;
    line-height: 20px;
  }
`;
